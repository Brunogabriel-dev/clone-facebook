import React from "react";
import * as C from "./styles";

const HeaderIcon = ({
  Icon,
  section = "section2",
  active = false,
  display = "default",
}) => {
  return (

  );
};

export default HeaderIcon;